---
title: "Домашнее задание 5"
author: "Фамилия, имя:"
output: html_document
---

###Задание 1

#1.1

Загрузите базы данных `boys.csv` и `girls.csv`. Обратите внимание на кодировку и разделитель столбцов + считайте, что текстовые переменные должны считываться как текст, а не как факторы (аргумент `stringsAsFactors`).

```{r 1.1}

```

#1.2

Используя библиотеку `dplyr`, создайте в базах имен столбец `gender` (тип `factor`), все значения которого равны "M" (для базы имен мальчиков) или "F" (для базы имен девочек). Соедините две базы имен по строкам и сохраните результат в переменную `total`. **Подсказка:** функция `rbind()`.

```{r 1.2}

```

#1.3

Выберите из базы данных `total` строки, соответствующие именам в векторах, сохраните результат в переменную `small`.

```{r 1.3}

```

#1.4

Создайте столбец `date`, используя код выше. Посмотрите на него, определите его тип.

```{r 1.4}

```

#1.5

Постройте с помощью библиотеки `ggplot2` линейный график (*line plot*), такой, чтобы по нему можно было определить динамику популярности имен девочек и мальчиков (временную шкалу мы уже создали в `date`). При этом постройте графики для женских и мужских имен в отдельных ячейках. Приведите график в приличный вид, согласно заданию.

```{r 1.5}

```

###Задание 2

```{r}
# добавьте столько ячеек с кодом, сколько вам потребуется
```

